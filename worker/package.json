{
  "name": "trinity-os-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute trinity-os-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
